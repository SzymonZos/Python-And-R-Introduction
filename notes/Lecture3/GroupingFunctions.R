# Grouping functions 
# apply -> accepts callback to a function; for arrays
# lapply -> for lists, returns a list
# sapply -> for lists as well, returns vector (named)
# mapply -> operations element-wised 
# tapply -> subsets, dunno BLOAT
# by XD
# aggregate